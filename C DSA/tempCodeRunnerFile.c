printf("Enter the number of nodes: ");
    // scanf(" %d", &n);
    // temp = (struct node *)malloc(sizeof(struct node));
    // if (n != 0)
    // {
    //     printf("\nEnter the data: ");
    //     scanf(" %d", &temp->data);
    // }
    // temp->next = NULL;
    // first = last = temp;